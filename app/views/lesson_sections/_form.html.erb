<%= form_with(model: lesson_section, local: true) do |form| %>
  <% if lesson_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesson_section.errors.count, "error") %> prohibited this lesson_section from being saved:</h2>

      <ul>
      <% lesson_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
